rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm oturum açmış kullanıcılar (request.auth != null) veriyi okuyabilir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
